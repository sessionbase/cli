// API base URL for backend calls
export const BASE_URL = process.env.SESSIONBASE_API_URL || 'https://api.sessionbase.ai';

// Web base URL for browser-based flows (login, etc.)
export const WEB_BASE_URL = process.env.SESSIONBASE_WEB_URL || 'https://sessionbase.ai';
